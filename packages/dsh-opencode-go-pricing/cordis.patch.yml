# dsh-opencode-go-pricing — profile bundle patch.
#
# `dsh plugin --profile web add dsh-opencode-go-pricing` installs this package
# and, because package.json declares `dsh.bundle.patch`, reconciles it into the
# profile's bundle layer stack automatically. This file is that bundle layer:
# it inserts the plugin entry with schema defaults; user overrides live in the
# `dsh-opencode-go-pricing` settings namespace (Web Settings → Plugins →
# Plugin configuration) or in `~/.dsh/settings.yaml`.
- insert:
    - id: dsh-opencode-go-pricing
      name: dsh-opencode-go-pricing
