# 依赖目录
node_modules/

# 日志与临时文件（本机开发痕迹，勿提交）
*.log
verify*.log
web.stdout.log
web.stderr.log
tmp-*.mjs
verify-*.mjs
probe-*.ps1

# 本机专用脚本（发布到 GitHub 时无意义）
restart-web.ps1

# pnpm 本地文件（插件包内不带 lockfile，装进 profile 后由 profile 生成）
pnpm-lock.yaml
pnpm-workspace.yaml

# 系统文件
.DS_Store
Thumbs.db
